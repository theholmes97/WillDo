<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="WillDo.ItemDetailPage" Title="{Binding Title}">
	<StackLayout Spacing="20" Padding="15">
		<Label Text="{Binding Goal.Title}" FontSize="Medium" />
		<Label Text="{Binding Goal.Description}" FontSize="Small" />
	</StackLayout>
</ContentPage>
